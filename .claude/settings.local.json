{
  "env": {
    "DB_SERVER": "10.34.145.21",
    "DB_NAME": "adv_hc",
    "DB_USER": "DataAnalysis-ReadOnly"
  },
  "permissions": {
    "allow": [
      "Bash(timeout:*)",
      "Bash(curl:*)",
      "Bash(python:*)",
      "Bash(ping:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -qE 'git (push --force|reset --hard|clean -fd)'; then echo 'WARNING: Destructive git command detected!' && exit 1; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'File edited: checking types...' && cd /d \"C:\\Program Files\\yieldrewind\\Yield-Rewind\" && npx tsc --noEmit 2>&1 || exit 0"
          }
        ]
      }
    ]
  }
}
